name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - run: uv sync --frozen --extra dev

      # prek runs the hooks and is the single source of truth for what "clean" means, so CI runs the
      # same hooks a commit does rather than a parallel list that can drift.
      - name: prek
        run: uv run --extra dev prek run --all-files --show-diff-on-failure
